/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['lh3.googleusercontent.com']
    }
};

// export const config = {
//     runtime: "edge",
//   };

export default nextConfig;
